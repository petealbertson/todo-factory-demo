<div id="<%= dom_id todo %>">
  <div>
    <strong>Title:</strong>
    <%= todo.title %>
  </div>

  <div>
    <strong>Completed:</strong>
    <%= todo.completed %>
  </div>

  <div>
    <%= button_to "Delete", todo, method: :delete, data: { turbo_confirm: "Are you sure?" } %>
  </div>

</div>
